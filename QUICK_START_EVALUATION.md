# Quick Start - RAG Evaluation

## เริ่มใช้งานเร็วๆ ใน 3 ขั้นตอน

### ขั้นตอนที่ 1: ติดตั้ง

```bash
pip install datasets ragas
```

### ขั้นตอนที่ 2: ตรวจสอบการตั้งค่า

```bash
python3 test_evaluation_setup.py
```

### ขั้นตอนที่ 3: รัน Evaluation

```bash
python3 evaluate_rag.py
```

เลือกโหมด 1 สำหรับการทดสอบเบื้องต้น

---

## คำสั่งที่ใช้บ่อย

### ทดสอบเร็ว (Quick Test)
```bash
python3 evaluate_rag.py
# เลือก 1 - Quick Evaluation
```

### เปรียบเทียบการตั้งค่าทั้งหมด
```bash
python3 evaluate_rag.py
# เลือก 2 - Compare All Configurations
```

### การตั้งค่าแบบกำหนดเอง
```bash
python3 evaluate_rag.py
# เลือก 3 - Custom Configuration
```

---

## การอ่านผลลัพธ์

### คะแนน (0.0 - 1.0)

| คะแนน | ความหมาย | สีสัญลักษณ์ |
|-------|---------|-----------|
| 0.8 - 1.0 | ดีเยี่ยม | 🟢 |
| 0.6 - 0.8 | ดี | 🟡 |
| 0.4 - 0.6 | พอใช้ | 🟠 |
| 0.0 - 0.4 | ต้องแก้ไข | 🔴 |

### Metrics

1. **Context Precision** - ความแม่นยำของ context ที่ดึงมา
2. **Context Recall** - context ครอบคลุมคำตอบหรือไม่
3. **Faithfulness** - คำตอบอิงจาก context จริงหรือไม่
4. **Answer Relevancy** - คำตอบตรงคำถามหรือไม่

---

## ตัวอย่างผลลัพธ์

```
📊 EVALUATION RESULTS
================================================================================
Context Precision: 0.8542  🟢 ดีเยี่ยม
Context Recall: 0.7321     🟡 ดี
Faithfulness: 0.9123       🟢 ดีเยี่ยม
Answer Relevancy: 0.8654   🟢 ดีเยี่ยม
================================================================================

💾 Results saved to: evaluation_results/quick_eval_full_stack_20260108_142730.json
```

---

## การแก้ปัญหาเบื้องต้น

### ❌ ModuleNotFoundError
```bash
pip install datasets ragas
```

### ❌ API Key ไม่พบ
ตรวจสอบไฟล์ `.env`:
```bash
cat .env
```

ต้องมี:
```
GEMINI_API_KEY=your_key_here
```

### ❌ Import Error
ตรวจสอบไฟล์ต้นฉบับ:
```bash
ls -la chatbot_v04_keywords.py
```

---

## ไฟล์ที่เกี่ยวข้อง

- `evaluate_rag.py` - สคริปต์หลัก
- `test_evaluation_setup.py` - ทดสอบการตั้งค่า
- `EVALUATION_GUIDE.md` - คู่มือฉบับเต็ม
- `evaluation_results/` - โฟลเดอร์ผลลัพธ์

---

## Tips

💡 ใช้ Quick Evaluation (โหมด 1) สำหรับทดสอบเบื้องต้น - เร็วและง่าย

💡 ใช้ Compare Configurations (โหมด 2) เมื่อต้องการวิเคราะห์เชิงลึก

💡 บันทึกผลลัพธ์เพื่อเปรียบเทียบในอนาคต

💡 ปรับ test cases ใน `create_test_dataset()` ให้เหมาะกับโดเมนของคุณ
