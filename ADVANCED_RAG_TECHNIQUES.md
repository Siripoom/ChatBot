# Advanced RAG Techniques Implementation

เอกสารนี้อธิบายเทคนิคขั้นสูงที่ถูกเพิ่มเข้ามาในระบบ RAG Chatbot เพื่อยกระดับคุณภาพการตอบคำถาม

## 📚 เทคนิคที่เพิ่มเข้ามา

### 1. 🎯 Re-Ranking with Cross-Encoder

**คืออะไร?**
- หลังจากค้นหา top-10 chunks ด้วย Hybrid Search (BM25 + Vector)
- ใช้ Cross-Encoder เพื่อ re-rank และเลือก top-3-5 ที่ดีที่สุด
- Cross-Encoder จะประเมินความเกี่ยวข้องระหว่าง query และ document แบบ pairwise

**ประโยชน์:**
- ✅ เพิ่มความแม่นยำในการเลือก context ที่เกี่ยวข้องที่สุด
- ✅ ลดการส่ง irrelevant context ให้ LLM
- ✅ ปรับปรุง context precision และ faithfulness

**Model ที่ใช้:**
- `cross-encoder/ms-marco-MiniLM-L-6-v2` - เร็วและมีประสิทธิภาพดี
- อีกตัวเลือก: `BAAI/bge-reranker-base` (สำหรับภาษาหลายภาษา)

**การใช้งาน:**
```python
# สร้าง knowledge base โดยเปิดใช้ re-ranker
kb = HybridKnowledgeBase(use_reranker=True)

# ปิดใช้ re-ranker
kb = HybridKnowledgeBase(use_reranker=False)
```

**วิธีทำงาน:**
1. Hybrid Search คืน 15 documents (top_k_for_rerank)
2. Cross-Encoder คำนวณ relevance score สำหรับแต่ละ document
3. Sort ตาม rerank_score และเลือก top-5
4. ส่ง top-5 ที่มีคุณภาพสูงให้ LLM

---

### 2. 🗜️ Context Compression

**คืออะไร?**
- บีบอัดเนื้อหาในแต่ละ chunk โดยให้ LLM คัดเลือกเฉพาะส่วนที่เกี่ยวข้องกับคำถาม
- กรองข้อมูลที่ไม่จำเป็นออก ก่อนส่งให้ LLM ตอบคำถาม

**ประโยชน์:**
- ✅ ลดจำนวน tokens ที่ส่งให้ LLM (ประหยัดค่าใช้จ่าย)
- ✅ ปรับปรุง context quality และ signal-to-noise ratio
- ✅ ช่วยให้ LLM focus ที่ข้อมูลที่สำคัญจริงๆ
- ✅ เพิ่ม answer relevancy และ faithfulness

**การใช้งาน:**
```python
# สร้าง chatbot โดยเปิดใช้ compression
chatbot = GeminiChatbot(api_key, kb, use_compression=True)

# ปิดใช้ compression
chatbot = GeminiChatbot(api_key, kb, use_compression=False)
```

**วิธีทำงาน:**
1. LLM วิเคราะห์แต่ละ context ว่าเกี่ยวข้องกับคำถามหรือไม่
2. ถ้าเกี่ยวข้อง (YES) → LLM สกัดเฉพาะส่วนที่เกี่ยวข้อง (2-3 ประโยค)
3. ถ้าไม่เกี่ยวข้อง (NO) → ตัดทิ้ง
4. ส่งเฉพาะ compressed context ให้ LLM ตอบคำถาม

---

### 3. 🔒 Strict RAG Prompt Engineering

**คืออะไร?**
- กำหนดกฎเกณฑ์ที่เข้มงวดให้ LLM ตอบจากข้อมูลในฐานความรู้เท่านั้น
- ห้ามเดา ห้ามใช้ความรู้ทั่วไป ห้ามสร้างข้อมูลขึ้นมาเอง (No Hallucination)

**ประโยชน์:**
- ✅ ลด hallucination (การสร้างข้อมูลปลอม)
- ✅ เพิ่ม faithfulness (ความตรงกับแหล่งอ้างอิง)
- ✅ ปรับปรุง source-grounded accuracy
- ✅ LLM จะบอกตรงๆ ว่า "ไม่มีข้อมูล" แทนที่จะเดา

**กฎที่เพิ่มเข้ามาใน Prompt:**
```
1. ✅ ตอบได้เฉพาะข้อมูลที่มีใน "ข้อมูลอ้างอิง" เท่านั้น
2. ❌ ห้ามเดาหรือสร้างข้อมูลที่ไม่มีในฐานความรู้
3. ❌ ห้ามใช้ความรู้ทั่วไปหรือข้อมูลภายนอกมาตอบ
4. ❌ ห้ามตอบคำถามที่ไม่มีข้อมูลในฐานความรู้
5. ✅ ถ้าไม่มีข้อมูล → บอกตรงๆ และแนะนำช่องทางติดต่อ
```

**ตัวอย่าง:**
- ❌ "คณะครุศาสตร์อุตสาหกรรมมีหลายหลักสูตร..." (เดาเอง)
- ✅ "ขออภัยค่ะ ข้อมูลนี้ไม่มีในระบบ ลองติดต่อที่ 02-555-2000 ค่ะ"

---

### 4. 📊 RAG Evaluation with Ragas

**คืออะไร?**
- Framework สำหรับประเมินคุณภาพของระบบ RAG อัตโนมัติ
- วัดได้ 4 metrics หลัก

**Metrics:**

#### 4.1 Context Precision (ความแม่นยำของ Context)
- วัดว่า context ที่ retrieve มาเกี่ยวข้องกับคำถามแค่ไหน
- คะแนนสูง = context ที่เลือกมามีประโยชน์จริง

#### 4.2 Context Recall (ความครอบคลุมของ Context)
- วัดว่า context ที่ retrieve มาครอบคลุมคำตอบที่ถูกต้องหรือไม่
- คะแนนสูง = context มีข้อมูลเพียงพอสำหรับตอบคำถาม

#### 4.3 Faithfulness (ความซื่อสัตย์ต่อแหล่งอ้างอิง)
- วัดว่าคำตอบที่ LLM สร้างขึ้นมา อิงจาก context จริงหรือไม่
- คะแนนสูง = LLM ตอบตาม context ไม่เดา

#### 4.4 Answer Relevancy (ความเกี่ยวข้องของคำตอบ)
- วัดว่าคำตอบตอบโจทย์คำถามหรือไม่
- คะแนนสูง = คำตอบตรงประเด็น ไม่อ้อมค้อม

**การใช้งาน:**
```bash
# รัน evaluation
python evaluate_rag.py

# เปรียบเทียบ configurations ต่างๆ
python evaluate_rag.py --compare
```

---

## 🚀 การใช้งาน

### ติดตั้ง Dependencies
```bash
pip install -r requirements.txt
```

### ใช้งานแบบ Full Stack (แนะนำ)
```python
from chatbot import HybridKnowledgeBase, GeminiChatbot
from dotenv import load_dotenv
import os

load_dotenv()
api_key = os.getenv('GEMINI_API_KEY')

# สร้าง knowledge base พร้อม re-ranker
kb = HybridKnowledgeBase(use_reranker=True)

# สร้าง chatbot พร้อม context compression
chatbot = GeminiChatbot(api_key, kb, use_compression=True)

# ถามคำถาม
response = chatbot.chat("หลักสูตรวิศวกรรมโยธาและการศึกษาเรียนกี่ปี")
print(response)
```

### ใช้งานแบบ Baseline (ไม่ใช้เทคนิคขั้นสูง)
```python
# ไม่ใช้ re-ranker และ compression
kb = HybridKnowledgeBase(use_reranker=False)
chatbot = GeminiChatbot(api_key, kb, use_compression=False)
```

---

## 📈 ผลลัพธ์ที่คาดหวัง

เมื่อใช้เทคนิคเหล่านี้ร่วมกัน คุณจะได้:

1. **Context Precision ↑** - context ที่เลือกมามีคุณภาพสูงขึ้น
2. **Context Recall ↑** - context ครอบคลุมคำตอบมากขึ้น
3. **Faithfulness ↑** - LLM ตอบตาม context มากขึ้น ลด hallucination
4. **Answer Relevancy ↑** - คำตอบตรงประเด็นมากขึ้น

### การเปรียบเทียบ Configurations

```
Configuration 1: Baseline
├── Re-ranker: ❌
├── Compression: ❌
└── Metrics: Baseline scores

Configuration 2: With Re-ranker
├── Re-ranker: ✅
├── Compression: ❌
└── Metrics: Improved context precision

Configuration 3: Full Stack (แนะนำ)
├── Re-ranker: ✅
├── Compression: ✅
└── Metrics: Best overall performance
```

---

## 🔧 การ Tune Parameters

### Re-ranking Parameters
```python
# ใน search_knowledge() method
top_k_for_rerank = min(n_results * 3, 15)  # เก็บ 3x ไว้ re-rank

# ปรับให้มากขึ้นถ้าต้องการ re-rank จาก pool ใหญ่ขึ้น
top_k_for_rerank = min(n_results * 5, 20)
```

### Compression Parameters
```python
# ในฟังก์ชัน compress_context()
# ปรับ prompt ให้เหมาะกับข้อมูล
compression_prompt = """..."""

# ปรับจำนวนประโยคที่สกัด
"สรุปสั้นๆ ภายใน 2-3 ประโยค"  # เปลี่ยนเป็น 3-5 ประโยค ถ้าต้องการมากขึ้น
```

### Hybrid Search Weights
```python
# ใน main() function
kb.search_knowledge(
    query,
    n_results=8,
    bm25_weight=0.4,    # ปรับน้ำหนัก BM25
    vector_weight=0.6   # ปรับน้ำหนัก Vector Search
)
```

---

## 🎯 Best Practices

1. **เริ่มจาก Baseline** - วัดประสิทธิภาพแบบไม่มีเทคนิคขั้นสูงก่อน
2. **เพิ่มเทคนิคทีละตัว** - ดูผลกระทบของแต่ละเทคนิค
3. **Evaluate อย่างสม่ำเสมอ** - ใช้ Ragas วัดผลทุกครั้งที่ทำการเปลี่ยนแปลง
4. **Monitor Cost** - Context Compression ช่วยลด tokens แต่ต้องเรียก LLM เพิ่ม
5. **Balance Speed vs Quality** - Re-ranking และ Compression ช่วยคุณภาพ แต่ใช้เวลามากขึ้น

---

## 🐛 Troubleshooting

### Re-ranker โหลดช้า
```python
# ใช้ model เล็กกว่า
self.reranker = CrossEncoder('cross-encoder/ms-marco-MiniLM-L-6-v2')  # เร็ว
# แทน
self.reranker = CrossEncoder('BAAI/bge-reranker-large')  # ช้ากว่า แต่แม่นกว่า
```

### Context Compression ใช้เวลานาน
```python
# ปิด compression สำหรับ production ที่ต้องการความเร็ว
chatbot = GeminiChatbot(api_key, kb, use_compression=False)

# หรือลดจำนวน contexts ที่ compress
relevant_knowledge = kb.search_knowledge(query, n_results=5)  # แทน 8
```

### Ragas Evaluation Failed
```bash
# ตรวจสอบว่าติดตั้ง dependencies ครบ
pip install ragas langchain langchain-community datasets

# ตรวจสอบ API key
export GEMINI_API_KEY="your-api-key"
```

---

## 📚 เอกสารอ้างอิง

- [Ragas Documentation](https://docs.ragas.io/)
- [Cross-Encoder for Re-ranking](https://www.sbert.net/examples/applications/cross-encoder/README.html)
- [LangChain Contextual Compression](https://python.langchain.com/docs/modules/data_connection/retrievers/contextual_compression/)
- [RAG Best Practices](https://www.llamaindex.ai/blog/a-guide-to-rag-evaluation-metrics-d1a8f3d98ef5)

---

## 🤝 Contributing

หากมีข้อเสนอแนะหรือพบปัญหา กรุณา:
1. เปิด Issue ใน GitHub
2. ส่ง Pull Request พร้อมคำอธิบาย
3. อัปเดตเอกสารนี้ให้ทันสมัย
